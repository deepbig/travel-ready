/*
import java.awt.*;
import java.awt.event.ActionEvent;
import org.junit.AfterClass;
import org.junit.BeforeClass;
import org.junit.Test;

import javax.swing.*;

import static org.junit.Assert.*;


public class RegistrationTest {

    public RegistrationTest() {
    }

    @BeforeClass
    public static void setUpClass() {
    }

    @AfterClass
    public static void tearDownClass() {
    }

    */
/**
     * Test of actionPerformed method, of class UserStay.
     *//*

    @Test
    public void testActionPerformed() {
        System.out.println("actionPerformed");
        try {
            ActionEvent e = null;
            */
/*JButton sub = new JButton("Submit");
            sub.setFont(new Font("Arial", Font.PLAIN, 15));
            sub.setSize(100, 20);
            sub.setLocation(150, 480);
            JCheckBox term = new JCheckBox("Accept Terms And Conditions.");
            term.setFont(new Font("Arial", Font.PLAIN, 12));
            term.setSize(250, 15);
            term.setLocation(150, 450);
            e.setSource(sub);*//*


            JButton reset = new JButton("Reset");
            reset.setFont(new Font("Arial", Font.PLAIN, 15));
            reset.setSize(100, 20);
            reset.setLocation(270, 480);
            e.setSource(reset);

            MyFrame instance = new MyFrame();
            instance.actionPerformed(e);
        } catch (Exception e) {
            fail("The test case is a prototype." + e);
        }
// TODO review the generated test code and remove the default call to fail.

    }

    */
/**
     * Test of main method, of class UserStay.
     *//*

    @Test
    public void testMain() throws Exception {
        System.out.println("main");
        String[] args = null;
        Registration.main(args);
// TODO review the generated test code and remove the default call to fail.
        fail("The test case is a prototype.");
    }

}
*/
